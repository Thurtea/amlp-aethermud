inherit "/lib/std/room.lpc";

void create() {
    ::create();
    set_short("Rose Walk - 2");
    set_long("Roses arch overhead here, their fragrance strong and sweet. The path continues east and west.");

    add_exit("west", "/domains/new_camelot/garden/east_1_1");
    add_exit("east", "/domains/new_camelot/garden/east_1_3");

    set_property("light", 2);
}
