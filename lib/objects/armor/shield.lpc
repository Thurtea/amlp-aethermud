// /lib/objects/armor/shield.lpc
inherit "/lib/std/object";

void create() {
    ::create();
    set_name("shield");
    set_id(({"shield", "buckler", "armor"}));
    set_short("a sturdy shield");
    set_long("This is a round wooden shield reinforced with steel bands.");
    set_weight(8);
    set_value(30);
}
